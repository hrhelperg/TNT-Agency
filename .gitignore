# dependencies
/node_modules

# next.js build output
/.next
/out

# typescript / next generated
next-env.d.ts
tsconfig.tsbuildinfo

# env
.env*.local

# misc
.DS_Store
*.log

# playwright browser QA artefacts (never commit payload traces)
/test-results
/playwright-report
/blob-report
/.playwright
release-evidence/

# Ad-hoc QA scripts written into the repo root so node can resolve
# @playwright/test from node_modules. Never part of a commit.
.qa-*.mjs
.chrome*.mjs
